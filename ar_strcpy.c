#include "main.h"
/**
 * ar_strcpy - copy from dest str to src str
 * @dest: destination string
 * @src: source string
 * Return: pointer to the new string
 */
char *ar_strcpy(char *dest, const char *src)
{
	return (strcpy(dest, src));
}
